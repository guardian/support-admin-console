import React from 'react';
import {
  Theme,
  createStyles,
  withStyles,
} from '@material-ui/core';

const styles = ({ spacing }: Theme) =>
  createStyles({
  });

interface TestEditorPropensityThresholdsProps {

}

const TestEditorPropensityThresholds: React.FC<TestEditorPropensityThresholdsProps> = (props) => {
  return (
    <div>
    </div>
  )
};

export default withStyles(styles)(TestEditorPropensityThresholds);
